version: '3.1'

services:
  filmflow-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=filmflow
      - POSTGRESQL_PASSWORD=password
      - POSTGRESQL_DATABASE=filmflow
